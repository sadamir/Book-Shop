const name = document.getElementById('name');
const forms = document.getElementById('forms');

forms.addEventListener('submit', (e) => {
  let messages = [];
  if (name.value === '' || name.value === null || name.value !== isString)
})
